# Docker Backup Service Configuration
# Copy this file to .env and configure your settings

# Telegram Bot Configuration (REQUIRED)
BOT_TOKEN=your_bot_token_from_botfather
BOT_DEST=your_chat_id_number

# Custom message prefix (optional)
CUST_MSG=Production Docker Backup

# Docker Volumes Configuration
# Comma-separated list of paths to scan for volumes
ROOT_DIR=/var/lib/docker/volumes

# Temporary directory for backup processing
TMP_DIR=/app/backups

# Database containers to dump (optional, auto-detection enabled)
# Comma-separated list of container names
DB_CONTAINERS=

# Backblaze B2 Configuration (REQUIRED - all files go to B2)
# Sign up at https://www.backblaze.com/b2/cloud-storage.html
S3_ENABLED=true
S3_BUCKET=your-backblaze-bucket-name
S3_PREFIX=docker-backups/

# Backblaze B2 Credentials
# Create in Backblaze B2 console -> App Keys
AWS_ACCESS_KEY_ID=your_backblaze_key_id
AWS_SECRET_ACCESS_KEY=your_backblaze_application_key

# Backblaze B2 Region (find in bucket settings)
# Common regions: us-west-001, us-west-002, us-west-004, eu-central-003
AWS_REGION=us-west-004


# Timezone for cron scheduling (optional)
TZ=UTC

# Example configurations for different setups:

# === Standard Docker Setup ===
# ROOT_DIR=/var/lib/docker/volumes

# === Docker Desktop on Linux ===
# ROOT_DIR=/var/lib/docker/volumes

# === Snap Docker ===
# ROOT_DIR=/var/snap/docker/common/var-lib-docker/volumes

# === Multiple Volume Locations ===
# ROOT_DIR=/var/lib/docker/volumes,/data/docker-volumes,/opt/volumes

# === Custom Database Containers ===
# DB_CONTAINERS=mysql-prod,postgres-analytics,redis-cache

# === Backblaze B2 Regions ===
# us-west-001 (US West - Oregon)
# us-west-002 (US West - California)  
# us-west-004 (US West - Arizona)
# us-east-001 (US East - Virginia)
# eu-central-003 (EU Central - Amsterdam)
# ap-southeast-002 (Asia Pacific - Singapore)

# === Timezone Examples ===
# TZ=America/New_York
# TZ=Europe/London  
# TZ=Asia/Tokyo
# TZ=UTC
# TZ=Indian/Maldives