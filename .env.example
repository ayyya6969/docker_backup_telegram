# Telegram Bot Configuration
# Get bot token from @BotFather on Telegram
BOT_TOKEN=123456789:ABCdefGHijklmnopQRSTuvwxyz

# Telegram destination chat ID
# Message your bot, then visit: https://api.telegram.org/bot<TOKEN>/getUpdates
BOT_DEST=1234567890

# Custom message to send before files list (optional)
# This will be prefixed to the timestamp
CUST_MSG=Production Server Backup

# Root directory for Docker volumes (comma-separated paths)
# Common locations: /var/lib/docker/volumes, /var/snap/docker/common/var-lib-docker/volumes
ROOT_DIR=/var/lib/docker/volumes,/data/docker-volumes

# Temporary directory for storing backups during processing
# Should have enough space for compressed volumes
TMP_DIR=/tmp/docker-backups

# Database containers to dump (optional, comma-separated)
# Script will auto-detect database containers, but you can specify additional ones
# Leave empty to rely on auto-detection only
DB_CONTAINERS=mysql_container,postgres_db,mariadb_app

# Examples of other common configurations:
#
# For Docker Desktop on Linux:
# ROOT_DIR=/var/lib/docker/volumes
#
# For snap Docker:
# ROOT_DIR=/var/snap/docker/common/var-lib-docker/volumes
#
# Multiple paths:
# ROOT_DIR=/var/lib/docker/volumes,/data/volumes,/mnt/storage/docker
#
# Custom temp directory:
# TMP_DIR=/data/tmp/backups
#
# Multiple database containers:
# DB_CONTAINERS=mysql_main,postgres_analytics,redis_cache,mongo_documents